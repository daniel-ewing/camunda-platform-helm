helm repo add opensearch https://opensearch-project.github.io/helm-charts/

cd /home/administrator/software/camunda/camunda-platform-8/daniel-ewing-camunda-platform-helm/opensearch
helm install opensearch opensearch/opensearch -f opensearch-values.yaml
helm install opensearch-dashboards opensearch/opensearch-dashboards -f opensearch-dashboard-values.yaml

export POD_NAME=$(kubectl get pods --namespace default -l "app.kubernetes.io/name=opensearch-dashboards,app.kubernetes.io/instance=opensearch-dashboards" -o jsonpath="{.items[0].metadata.name}")
export CONTAINER_PORT=$(kubectl get pod --namespace default $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
echo "Visit http://127.0.0.1:8080 to use your application"
kubectl --namespace default port-forward $POD_NAME 8080:$CONTAINER_PORT

cd /home/administrator/software/camunda/camunda-platform-8/daniel-ewing-camunda-platform-helm/dev-10.0.x-tests
helm install dev camunda/camunda-platform \
                 -f separated-ingress-opensearch-values.yaml \
                 --version 10.0.2

helm upgrade dev camunda/camunda-platform \
                 -f separated-ingress-opensearch-values.yaml \
                 --version 10.0.2

